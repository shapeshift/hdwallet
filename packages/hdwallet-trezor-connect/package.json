{
  "name": "@shapeshiftoss/hdwallet-trezor-connect",
  "version": "1.15.1",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "dependencies": {
    "@shapeshiftoss/hdwallet-core": "^1.15.1",
    "@shapeshiftoss/hdwallet-trezor": "^1.15.1",
    "trezor-connect": "^7.0.5"
  },
  "devDependencies": {
    "typescript": "^4.3.2"
  },
  "engines": {
    "node": "^10 || ^12 || ^14"
  }
}
