{
  "name": "@shapeshiftoss/hdwallet-walletconnectv2",
  "version": "1.62.13-gridplus-validation.17",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist node_modules",
    "dev": "yarn tsc --build --watch",
    "prepublishOnly": "rm -rf dist && yarn build"
  },
  "dependencies": {
    "@shapeshiftoss/hdwallet-core": "^1.62.13-gridplus-validation.17",
    "@walletconnect/ethereum-provider": "^2.20.2",
    "@walletconnect/modal": "^2.6.2",
    "ethers": "^5.6.5"
  }
}
