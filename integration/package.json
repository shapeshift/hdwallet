{
  "name": "integration",
  "version": "2.11.2",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist .cache",
    "test": "yarn jest --verbose",
    "dev": "lerna run test --scope integration --parallel --include-filtered-dependencies"
  },
  "dependencies": {
    "@shapeshift/hdwallet-core": "^0.1.0",
    "@shapeshift/hdwallet-keepkey": "^0.1.0",
    "@shapeshift/hdwallet-keepkey-nodewebusb": "^0.1.0",
    "@shapeshift/hdwallet-keepkey-tcp": "^0.1.0",
    "@shapeshift/hdwallet-trezor": "^0.1.0",
    "debug": "^4.1.1",
    "parcel-bundler": "^1.11.2",
    "whatwg-fetch": "^2.0.4"
  },
  "devDependencies": {
    "@types/debug": "^4.1.2",
    "@types/jest": "^24.0.11",
    "jest": "^24.6.0",
    "ts-jest": "^24.0.1",
    "typescript": "^3.4.1"
  }
}
